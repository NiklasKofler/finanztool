{
  "name": "finanztool",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "npm --prefix app run start",
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build",
    "lint": "npm --prefix app run lint",
    "agent": "npm --prefix automation run start",
    "backfill:summaries": "npm --prefix automation run backfill:summaries",
    "deploy:firestore-rules": "npm --prefix automation run deploy:firestore-rules",
    "serve:app": "npm --prefix automation run serve:app",
    "serve:documents": "npm --prefix automation run serve:documents",
    "sync:document-storage": "npm --prefix automation run sync:document-storage",
    "auth:sparkasse-george": "npm --prefix automation run auth:sparkasse-george",
    "auth:sparkasse-george:open": "npm --prefix automation run auth:sparkasse-george:open",
    "reconcile:sparkasse-george": "npm --prefix automation run reconcile:sparkasse-george",
    "sync:sparkasse-george": "npm --prefix automation run sync:sparkasse-george",
    "auth:bank-accounts": "npm --prefix automation run auth:bank-accounts",
    "auth:bank-accounts:open": "npm --prefix automation run auth:bank-accounts:open",
    "reconcile:bank-accounts": "npm --prefix automation run reconcile:bank-accounts",
    "reconcile:bank-accounts:backfill": "npm --prefix automation run reconcile:bank-accounts:backfill",
    "sync:bank-accounts": "npm --prefix automation run sync:bank-accounts",
    "sync:bank-accounts:backfill": "npm --prefix automation run sync:bank-accounts:backfill",
    "install:all": "npm --prefix app install && npm --prefix automation install",
    "ftd": "zsh bin/ftd",
    "fts": "zsh bin/fts",
    "ftu": "zsh bin/ftu",
    "ftp": "zsh bin/ftp",
    "ft:install": "zsh automation/src/install-ft-shortcuts.sh"
  },
  "engines": {
    "node": ">=20.19"
  }
}
